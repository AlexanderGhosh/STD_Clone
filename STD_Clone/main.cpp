#include <array>
#include <vector>
#include <list>
#include "Containers/array.h"
#include "Containers/vector.h"
#include "Containers/list.h"
#include "MemoryPool.h"

int main() {
	alx::list<int> a;
	std::list<int> b;

	a.push_back(0);
	a.push_back(1);
	a.push_back(2);

	b.push_back(0);
	b.push_back(1);
	b.push_back(2);

	int fhd = 0;
}